aReady(function(){
  try {
    _satellite.notify('@@@ IN JSTAG');
    _satellite.notify('@@@:'+ _a.pickupStationId);
    _satellite.notify('@@@:'+ _a.dropoffStationId);
    _satellite.notify('@@@:'+ _a.reservationPickupDateTime);
    _satellite.notify('@@@:'+ _a.reservationDropoffDateTime);
    _satellite.notify('@@@@ JSTAG= ' + typeof jstag );
    if(jstag !== 'undefined'){
      _a.visID = _satellite.getVar('s_mcvID');
			jstag.send('alamo', _a);
    }
  }catch(e){ _satellite.notify('@@@@: ' + e ); }
}, false);
